apiVersion: v1
kind: ConfigMap
metadata:
  name: analytics-postgres-config
  namespace: {{ .Values.namespace | quote }}
data:
  POSTGRES_DB: {{ .Values.analytics_postgres.database | quote }}
  POSTGRES_USER: {{ .Values.analytics_postgres.user | quote }}