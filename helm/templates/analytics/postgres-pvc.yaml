apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: {{ .Values.analytics_postgres.persistence.claimName }}
  namespace: {{ .Values.namespace | quote }}
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: local-path
  resources:
    requests:
      storage: {{ .Values.analytics_postgres.persistence.size }}