apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: {{ .Values.analytics.postgres.persistence.claimName }}
  namespace: {{ .Values.namespace | quote }}
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: local-path
  resources:
    requests:
      storage: {{ .Values.analytics.postgres.persistence.size }}