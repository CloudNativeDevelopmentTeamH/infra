apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: auth-postgres-statefulset
  namespace: {{ .Values.namespace | quote }}
spec:
  serviceName: {{ .Values.auth.postgres.serviceName }}

  replicas: 1
  selector:
    matchLabels:
      app: auth-postgres

  template:
    metadata:
      labels:
        app: auth-postgres
    spec:
      volumes:
      - name: data
        persistentVolumeClaim:
          claimName: {{ .Values.auth.postgres.persistence.claimName }}

      containers:
      - name: postgres
        image: "{{ .Values.auth.postgres.image.repository }}:{{ .Values.auth.postgres.image.tag }}"
        ports:
        - containerPort: {{ .Values.auth.postgres.port }}
          name: postgres
        volumeMounts:
          - name: data
            mountPath: /var/lib/postgresql/data
        envFrom:
        - configMapRef:
            name: {{ .Values.auth.postgres.configmap.name }}
        env:
        - name: PGDATA
          value: /var/lib/postgresql/data/pgdata
        - name: POSTGRES_PASSWORD
          valueFrom:
            secretKeyRef:
              name: {{ .Values.auth.secrets.name }}
              key: DB_PASSWORD
        - name: POSTGRES_USER
          valueFrom:
            secretKeyRef:
              name: {{ .Values.auth.secrets.name }}
              key: DB_USERNAME
