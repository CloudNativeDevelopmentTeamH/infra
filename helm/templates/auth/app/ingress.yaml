apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: auth-ingress
  namespace: {{ .Values.namespace | quote  }}
spec:
  entryPoints:
    - websecure
  routes:
    - kind: Rule
      match: Host(`{{ .Values.auth.host }}`)
      services:
        - name: "{{ .Values.auth.name }}-service"
          port: {{ .Values.auth.port }}
