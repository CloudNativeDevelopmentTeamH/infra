apiVersion: v1
kind: Secret
metadata:
  name: {{ .Values.backend.pullSecret.name }}
  namespace: {{ .Values.namespace | quote  }}
type: kubernetes.io/dockerconfigjson
stringData:
  .dockerconfigjson: {{ .Values.backend.pullSecret.dockerconfigjson | quote }}
