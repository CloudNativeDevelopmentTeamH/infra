apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: focus-postgres-statefulset
  namespace: {{ .Values.namespace | quote }}
spec:
  serviceName: {{ .Values.focus.postgres.serviceName }}

  replicas: 1
  selector:
    matchLabels:
      app: postgres

  template:
    metadata:
      labels:
        app: postgres
    spec:
      volumes:
      - name: data
        persistentVolumeClaim:
          claimName: {{ .Values.focus.postgres.persistence.claimName }}

      containers:
      - name: postgres
        image: "{{ .Values.focus.postgres.image.repository }}:{{ .Values.focus.postgres.image.tag }}"
        ports:
        - containerPort: {{ .Values.focus.postgres.port }}
        volumeMounts:
          - name: data
            mountPath: /var/lib/postgresql/data
        envFrom:
        - configMapRef:
            name: postgres-config
        env:
        - name: PGDATA
          value: /var/lib/postgresql/data/pgdata
        - name: POSTGRES_PASSWORD
          valueFrom:
            secretKeyRef:
              name: {{ .Values.focus.secrets.name }}
              key: DB_PASSWORD