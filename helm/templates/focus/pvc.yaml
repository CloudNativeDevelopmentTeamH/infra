apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: {{ .Values.focus.postgres.persistence.claimName }}
  namespace: {{ .Values.namespace | quote }}
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: {{ .Values.focus.postgres.persistence.storageClassName | default "ebs-sc" }}
  resources:
    requests:
      storage: {{ .Values.focus.postgres.persistence.size | default "1Gi" }}